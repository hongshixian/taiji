# 太极环境变量模板 — 复制为 .env 后修改

# Flask
FLASK_ENV=development
SECRET_KEY=change-me-to-a-random-string

# 仅供源码直接启动后端时使用；Compose 固定连接内置 PostgreSQL
DATABASE_URL=sqlite:///../data/taiji.db

# 仅供源码直接启动后端时使用；Compose 固定连接内置 Redis
REDIS_URL=redis://localhost:6379/0

# 认证模式：生产使用 Keycloak OIDC；legacy 仅保留旧环境应急回滚
AUTH_MODE=oidc

# 任务执行日志
TASK_LOG_ROOT=../app_logs

# 仅 AUTH_MODE=legacy 使用的 JWT 回滚配置
JWT_SECRET_KEY=change-me-to-a-random-jwt-secret
JWT_ACCESS_TOKEN_EXPIRES=1800
JWT_REFRESH_TOKEN_EXPIRES=604800

# 仅 AUTH_MODE=legacy 的应急回滚模式使用；OIDC 初始管理员由 iam-bootstrap 创建
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@taiji.local
# ADMIN_PASSWORD=

# 太极统一登录（Keycloak 仅为太极内部认证内核）
KEYCLOAK_VERSION=26.7.0
KEYCLOAK_DB=keycloak
TAIJI_PUBLIC_URL=http://localhost:28080
IAM_PUBLIC_URL=http://localhost:28080/iam
IAM_INTERNAL_URL=http://localhost:28080/iam
# 允许发起 OIDC 登录的平台地址，逗号分隔；至少包含 TAIJI_PUBLIC_URL
TAIJI_PUBLIC_URLS=http://localhost:28080
TAIJI_PORT=28080
# HTTPS 在反向代理终止并转发 HTTP 时使用 xforwarded；TLS 透传时不要设置。
KC_PROXY_HEADERS=xforwarded
KEYCLOAK_BOOTSTRAP_ADMIN_USERNAME=kcadmin
KEYCLOAK_BOOTSTRAP_ADMIN_PASSWORD=dev-only-change-me
TAIJI_OIDC_CLIENT_SECRET=taiji-web-dev-secret
SESSION_IDLE_SECONDS=7200
SESSION_ABSOLUTE_SECONDS=86400
# 本地 HTTP 开发为 false；生产 HTTPS 必须设为 true
SESSION_COOKIE_SECURE=false

# 平台超级管理员一次性引导（与 Keycloak 基础设施管理员不同）
# 新环境会创建用户名 admin、生成随机临时密码并仅输出到登录引导容器日志。
IAM_BOOTSTRAP_ENABLED=true
IAM_REALM_RECONCILE_ENABLED=true
IAM_BOOTSTRAP_ADMIN_EMAIL=admin@taiji.local
